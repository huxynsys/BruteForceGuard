"""Phase 8 reliability test package.

Tests in this package deliberately break infrastructure boundaries
(database, threat-intelligence provider, reputation, MITRE) and assert the
detection pipeline keeps working.
"""