# ---------------------------------------------------------------------------
# Production validation environment (throwaway - delete after use)
# ---------------------------------------------------------------------------
POSTGRES_PASSWORD=change-this-password
DATABASE_URL=postgresql+psycopg://bfg_user:change-this-password@postgres:5432/bruteforceguard

APP_ENV=production
LOG_LEVEL=INFO

# Production requires an explicit, non-wildcard origin list.
CORS_ALLOWED_ORIGINS=http://localhost:8080

VITE_API_BASE_URL=
HTTP_PORT=8080
ENV_FILE=.env.prod.validation